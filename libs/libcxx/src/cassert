#ifndef _STD_CASSERT_HPP
#define _STD_CASSERT_HPP

#include <cstdlib>

#ifdef NDEBUG
#define assert(cond) ((void)0)
#else // NDEBUG

#define assert(cond)  \
    if(!(cond)) {     \
        std::abort(); \
    }
#endif // NDEBUG

#endif // _STD_CASSERT_HPP
